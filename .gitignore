node_modules/
dist/
.wrangler/
*.tsbuildinfo

# Local secrets. SESSION_SECRET lives in `wrangler secret put`, never in git.
.dev.vars
.env
.env.local

# Playwright output
test-results/
playwright-report/

# Graphify's code map and Obsidian vault. Built on your own machine with
# `graphify update .` — see docs/graphify.md. Not committed: it is a derived
# view of this repository, regenerated in seconds, and the repository is public.
graphify-out/
